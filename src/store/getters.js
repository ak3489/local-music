/*
 * @LastEditors: gcz
 */
const getters = {
    // user: state => state.user.users,
    // userLength: state => state.user.users.length,
    vuexSearchType: state => state.music.vuexSearchType,
    vuexSearchNo: state => state.music.vuexSearchNo,
}

export default getters